$(".schools").html("<%= escape_javascript(render("fetchAll")) %>");

var client = new Faye.Client('http://localhost:9292/faye');
client.publish('/admin/schools/<%= @o_single.id %>/edit', {
  sid: '<%= @o_single.id %>'
});

client.publish('/schools', { text: 'schools' });
